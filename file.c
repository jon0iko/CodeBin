#include <stdio.h>
typedef long long int lli;

int main()
{
    // FILE* fp;
    // fp = fopen("file.txt", "r");
    // char data[50];
    // fgets(data, 50, fp);
    // printf("%s\n",data);
    // fclose(fp);
    printf("%s", __DATE__);
    return 0;
}