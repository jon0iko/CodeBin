#include <stdio.h>
typedef long long int lli;

int main()
{
    while (1)
    {
        int k;
        char s[71];
        scanf("%d", &k);
        if (k==0)
        {
            break;
        }
        fgets(s,71,stdin);
        
        
    }
    
    return 0;
}